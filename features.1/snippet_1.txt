User UI (Upload mesh / DICOM) 
       ↓
Flask Backend (Load & Voxelize mesh / Convert DICOM) 
       ↓
Store voxel grids + properties
       ↓
Physics sim (FDTD) run on voxel grid 
       ↓
Send results to frontend for visualization (Three.js / Canvas)
       ↓
User edits properties / re-runs simulation / saves configs
